<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.thinkgem.jeesite.modules.zjvideo.dao.ClazzDao">
    
	<sql id="clazzColumns">
		a.id AS "id",
		a.name AS "name",
		a.typeid AS "typeid",
		a.parent_id AS "parent.id",
		a.class_flag AS "classFlag",
		a.icount AS "icount",
		a.iorder AS "iorder",
		a.addtime AS "addtime",
		a.edittime AS "edittime",
		a.slock AS "slock",
		a.create_by AS "createBy.id",
		a.create_date AS "createDate",
		a.update_by AS "updateBy.id",
		a.update_date AS "updateDate",
		a.remarks AS "remarks",
		a.del_flag AS "delFlag",
		a.parent_ids AS "parentIds",
		a.sort AS "sort"
	</sql>
	
	<sql id="clazzJoins">
	</sql>
    
	<select id="get" resultType="Clazz">
		SELECT 
			<include refid="clazzColumns"/>
		FROM t_clazz a
		<include refid="clazzJoins"/>
		WHERE a.id = #{id}
	</select>
	
	<select id="findList" resultType="Clazz">
		SELECT 
			<include refid="clazzColumns"/>
		FROM t_clazz a
		<include refid="clazzJoins"/>
		<where>
			a.del_flag = #{DEL_FLAG_NORMAL}
			<if test="name != null and name != ''">
				AND a.name LIKE 
					<if test="dbName == 'oracle'">'%'||#{name}||'%'</if>
					<if test="dbName == 'mssql'">'%'+#{name}+'%'</if>
					<if test="dbName == 'mysql'">concat('%',#{name},'%')</if>
			</if>
			<if test="parent != null and parent.id != null and parent.id != ''">
				AND a.parent_id = #{parent.id}
			</if>
			<if test="parentIds != null and parentIds != ''">
				AND a.parent_ids LIKE 
					<if test="dbName == 'oracle'">'%'||#{parentIds}||'%'</if>
					<if test="dbName == 'mssql'">'%'+#{parentIds}+'%'</if>
					<if test="dbName == 'mysql'">concat('%',#{parentIds},'%')</if>
			</if>
		</where>
		ORDER BY a.sort ASC
	</select>
	
	<select id="findAllList" resultType="Clazz">
		SELECT 
			<include refid="clazzColumns"/>
		FROM t_clazz a
		<include refid="clazzJoins"/>
		<where>
			a.del_flag = #{DEL_FLAG_NORMAL}
		</where>
		ORDER BY a.sort ASC
	</select>
	
	<select id="findByParentIdsLike" resultType="Clazz">
		SELECT
			a.id,
			a.parent_id AS "parent.id",
			a.parent_ids
		FROM t_clazz a
		<include refid="clazzJoins"/>
		<where>
			a.del_flag = #{DEL_FLAG_NORMAL}
			AND a.parent_ids LIKE #{parentIds}
		</where>
		ORDER BY a.sort ASC
	</select>
	
	<insert id="insert" useGeneratedKeys="true" keyProperty="id">
		INSERT INTO t_clazz(
			id,
			name,
			typeid,
			parent_id,
			class_flag,
			icount,
			iorder,
			addtime,
			edittime,
			slock,
			create_by,
			create_date,
			update_by,
			update_date,
			remarks,
			del_flag,
			parent_ids,
			sort
		) VALUES (
			default,
			#{name},
			#{typeid},
			#{parent.id},
			#{classFlag},
			#{icount},
			#{iorder},
			#{addtime},
			#{edittime},
			#{slock},
			#{createBy.id},
			#{createDate},
			#{updateBy.id},
			#{updateDate},
			#{remarks},
			#{delFlag},
			#{parentIds},
			#{sort}
		)
	</insert>
	
	<update id="update">
		UPDATE t_clazz SET 	
			name = #{name},
			typeid = #{typeid},
			parent_id = #{parent.id},
			class_flag = #{classFlag},
			icount = #{icount},
			iorder = #{iorder},
			addtime = #{addtime},
			edittime = #{edittime},
			slock = #{slock},
			update_by = #{updateBy.id},
			update_date = #{updateDate},
			remarks = #{remarks},
			parent_ids = #{parentIds},
			sort = #{sort}
		WHERE id = #{id}
	</update>
	
	<update id="updateParentIds">
		UPDATE t_clazz SET 
			parent_id = #{parent.id}, 
			parent_ids = #{parentIds}
		WHERE id = #{id}
	</update>
	
	<update id="delete">
		UPDATE t_clazz SET 
			del_flag = #{DEL_FLAG_DELETE}
		WHERE id = #{id} OR parent_ids LIKE '%,'||#{id}||',%'
	</update>
	
</mapper>