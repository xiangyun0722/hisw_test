<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.thinkgem.jeesite.modules.live.dao.BusinessSystemDao">
    
	<sql id="businessSystemColumns">
		a.id AS "id",
		a.name AS "name",
		a.logo AS "logo",
		a.public_key AS "publicKey",
		a.create_by AS "createBy.id",
		a.create_date AS "createDate",
		a.update_by AS "updateBy.id",
		a.update_date AS "updateDate",
		a.remarks AS "remarks",
		a.del_flag AS "delFlag"
	</sql>
	
	<sql id="businessSystemJoins">
	</sql>
    
	<select id="get" resultType="BusinessSystem">
		SELECT 
			<include refid="businessSystemColumns"/>
		FROM live_business_system a
		<include refid="businessSystemJoins"/>
		WHERE a.id = #{id}
	</select>
	
	<select id="getByPublicKey" resultType="BusinessSystem">
		SELECT 
			<include refid="businessSystemColumns"/>
		FROM live_business_system a
		<include refid="businessSystemJoins"/>
		WHERE a.public_key = #{publicKey}
	</select>
	
	<select id="findList" resultType="BusinessSystem">
		SELECT 
			<include refid="businessSystemColumns"/>
		FROM live_business_system a
		<include refid="businessSystemJoins"/>
		<where>
			a.del_flag = #{DEL_FLAG_NORMAL}
			<if test="name != null and name != ''">
				AND a.name LIKE 
					<if test="dbName == 'oracle'">'%'||#{name}||'%'</if>
					<if test="dbName == 'mssql'">'%'+#{name}+'%'</if>
					<if test="dbName == 'mysql'">concat('%',#{name},'%')</if>
			</if>
			<if test="publicKey != null and publicKey != ''">
				AND a.public_key LIKE 
					<if test="dbName == 'oracle'">'%'||#{publicKey}||'%'</if>
					<if test="dbName == 'mssql'">'%'+#{publicKey}+'%'</if>
					<if test="dbName == 'mysql'">concat('%',#{publicKey},'%')</if>
			</if>
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
				ORDER BY a.update_date DESC
			</otherwise>
		</choose>
	</select>
	
	<select id="findAllList" resultType="BusinessSystem">
		SELECT 
			<include refid="businessSystemColumns"/>
		FROM live_business_system a
		<include refid="businessSystemJoins"/>
		<where>
			a.del_flag = #{DEL_FLAG_NORMAL}
		</where>		
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
				ORDER BY a.update_date DESC
			</otherwise>
		</choose>
	</select>
	
	<insert id="insert">
		INSERT INTO live_business_system(
			id,
			name,
			logo,
			public_key,
			create_by,
			create_date,
			update_by,
			update_date,
			remarks,
			del_flag
		) VALUES (
			#{id},
			#{name},
			#{logo},
			#{publicKey},
			#{createBy.id},
			#{createDate},
			#{updateBy.id},
			#{updateDate},
			#{remarks},
			#{delFlag}
		)
	</insert>
	
	<update id="update">
		UPDATE live_business_system SET 	
			name = #{name},
			logo = #{logo},
			public_key = #{publicKey},
			update_by = #{updateBy.id},
			update_date = #{updateDate},
			remarks = #{remarks}
		WHERE id = #{id}
	</update>
	
	<update id="delete">
		UPDATE live_business_system SET 
			del_flag = #{DEL_FLAG_DELETE}
		WHERE id = #{id}
	</update>
	
</mapper>